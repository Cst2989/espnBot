{
  "name": "espnbot",
  "version": "1.0.0",
  "description": "a bot that changes the hour of the espn waiver",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "./node_modules/.bin/cypress open",
    "cy:run": "./node_modules/.bin/cypress run"
  },
  "author": "Neciu Dan",
  "license": "ISC",
  "dependencies": {
    "cypress": "^3.4.1",
    "fs": "0.0.1-security",
    "node-cron": "^2.0.3",
    "shelljs": "^0.8.3"
  }
}
